# QA Brain — synthetic capture demo

## Distilled conversation: 2026-01-15

[user] Our synthetic account-switching test fails when two browser contexts share a saved session.
[assistant] The example needs session isolation: use a separate authentication state for each test role.
[user] Demo-only placeholder: PASSWORD=<redacted> Record the observed behavior and how to retest it.
[assistant] Draft a reusable lesson with evidence and a limitation: this fixture does not prove how any real application behaves.

Rejected malformed records: 0

## Retrieve a reusable lesson

Query: session isolation
Citation: vault/reflections/session-isolation.md
Status: synthetic-example; review before applying to real work.

Capture is automated; promotion into a trusted lesson requires review. No LLM or external service was called.
